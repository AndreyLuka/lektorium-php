{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <h1>Edit Task</h1>
        <div class="row">
            <div class="col">
                {{ form(form,  {'attr': {'novalidate': 'novalidate'}}) }}
            </div>
            <div class="col">
                <p>Task owner: {{ taskUser.email }}</p>
                <p>Current user: {{ app.user.email }}</p>
            </div>
        </div>
    </div>
{% endblock %}
